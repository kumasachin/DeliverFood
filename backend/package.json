{
  "name": "toptal-food-delivery-be",
  "version": "1.0.0",
  "main": "bin/server.js",
  "private": true,
  "scripts": {
    "start:dev": "node --watch src/commands/serve.js",
    "start": "node src/commands/serve.js",
    "db:reset": "node src/commands/dbReset.js",
    "test": "jest --testTimeout=3000",
    "test:watch": "jest --watch --testTimeout=3000"
  },
  "dependencies": {
    "better-sqlite3": "^8.7.0",
    "express": "^4.18.2",
    "joi": "^17.12.0",
    "jsonwebtoken": "^9.0.0",
    "swagger-ui-express": "^4.6.0",
    "uuid": "^9.0.0",
    "yaml": "^2.2.1"
  },
  "devDependencies": {
    "jest": "^29.3.1",
    "supertest": "^6.3.1"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  }
}
